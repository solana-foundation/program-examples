/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    fixDecoderSize,
    fixEncoderSize,
    getBytesDecoder,
    getBytesEncoder,
    getStructDecoder,
    getStructEncoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
    type WritableSignerAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findConfigPda } from '../pdas';
import { ABL_TOKEN_PROGRAM_ADDRESS } from '../programs';

export const INIT_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([23, 235, 115, 232, 168, 96, 1, 231]);

export function getInitConfigDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_CONFIG_DISCRIMINATOR);
}

export type InitConfigInstruction<
    TProgram extends string = typeof ABL_TOKEN_PROGRAM_ADDRESS,
    TAccountPayer extends string | AccountMeta<string> = string,
    TAccountConfig extends string | AccountMeta<string> = string,
    TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountPayer extends string
                ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
                : TAccountPayer,
            TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
            TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
            ...TRemainingAccounts,
        ]
    >;

export type InitConfigInstructionData = { discriminator: ReadonlyUint8Array };

export type InitConfigInstructionDataArgs = {};

export function getInitConfigInstructionDataEncoder(): FixedSizeEncoder<InitConfigInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), value => ({
        ...value,
        discriminator: INIT_CONFIG_DISCRIMINATOR,
    }));
}

export function getInitConfigInstructionDataDecoder(): FixedSizeDecoder<InitConfigInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getInitConfigInstructionDataCodec(): FixedSizeCodec<
    InitConfigInstructionDataArgs,
    InitConfigInstructionData
> {
    return combineCodec(getInitConfigInstructionDataEncoder(), getInitConfigInstructionDataDecoder());
}

export type InitConfigAsyncInput<
    TAccountPayer extends string = string,
    TAccountConfig extends string = string,
    TAccountSystemProgram extends string = string,
> = {
    payer: TransactionSigner<TAccountPayer>;
    config?: Address<TAccountConfig>;
    systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitConfigInstructionAsync<
    TAccountPayer extends string,
    TAccountConfig extends string,
    TAccountSystemProgram extends string,
    TProgramAddress extends Address = typeof ABL_TOKEN_PROGRAM_ADDRESS,
>(
    input: InitConfigAsyncInput<TAccountPayer, TAccountConfig, TAccountSystemProgram>,
    config?: { programAddress?: TProgramAddress },
): Promise<InitConfigInstruction<TProgramAddress, TAccountPayer, TAccountConfig, TAccountSystemProgram>> {
    // Program address.
    const programAddress = config?.programAddress ?? ABL_TOKEN_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        payer: { value: input.payer ?? null, isWritable: true },
        config: { value: input.config ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Resolve default values.
    if (!accounts.config.value) {
        accounts.config.value = await findConfigPda();
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('payer', accounts.payer),
            getAccountMeta('config', accounts.config),
            getAccountMeta('systemProgram', accounts.systemProgram),
        ],
        data: getInitConfigInstructionDataEncoder().encode({}),
        programAddress,
    } as InitConfigInstruction<TProgramAddress, TAccountPayer, TAccountConfig, TAccountSystemProgram>);
}

export type InitConfigInput<
    TAccountPayer extends string = string,
    TAccountConfig extends string = string,
    TAccountSystemProgram extends string = string,
> = {
    payer: TransactionSigner<TAccountPayer>;
    config: Address<TAccountConfig>;
    systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitConfigInstruction<
    TAccountPayer extends string,
    TAccountConfig extends string,
    TAccountSystemProgram extends string,
    TProgramAddress extends Address = typeof ABL_TOKEN_PROGRAM_ADDRESS,
>(
    input: InitConfigInput<TAccountPayer, TAccountConfig, TAccountSystemProgram>,
    config?: { programAddress?: TProgramAddress },
): InitConfigInstruction<TProgramAddress, TAccountPayer, TAccountConfig, TAccountSystemProgram> {
    // Program address.
    const programAddress = config?.programAddress ?? ABL_TOKEN_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        payer: { value: input.payer ?? null, isWritable: true },
        config: { value: input.config ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('payer', accounts.payer),
            getAccountMeta('config', accounts.config),
            getAccountMeta('systemProgram', accounts.systemProgram),
        ],
        data: getInitConfigInstructionDataEncoder().encode({}),
        programAddress,
    } as InitConfigInstruction<TProgramAddress, TAccountPayer, TAccountConfig, TAccountSystemProgram>);
}

export type ParsedInitConfigInstruction<
    TProgram extends string = typeof ABL_TOKEN_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        payer: TAccountMetas[0];
        config: TAccountMetas[1];
        systemProgram: TAccountMetas[2];
    };
    data: InitConfigInstructionData;
};

export function parseInitConfigInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedInitConfigInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 3) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 3,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { payer: getNextAccount(), config: getNextAccount(), systemProgram: getNextAccount() },
        data: getInitConfigInstructionDataDecoder().decode(instruction.data),
    };
}
