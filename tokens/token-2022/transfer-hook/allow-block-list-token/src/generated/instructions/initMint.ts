/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    addDecoderSizePrefix,
    addEncoderSizePrefix,
    combineCodec,
    fixDecoderSize,
    fixEncoderSize,
    getAddressDecoder,
    getAddressEncoder,
    getBytesDecoder,
    getBytesEncoder,
    getStructDecoder,
    getStructEncoder,
    getU32Decoder,
    getU32Encoder,
    getU64Decoder,
    getU64Encoder,
    getU8Decoder,
    getU8Encoder,
    getUtf8Decoder,
    getUtf8Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type Codec,
    type Decoder,
    type Encoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
    type WritableSignerAccount,
} from '@solana/kit';
import {
    getAccountMetaFactory,
    getAddressFromResolvedInstructionAccount,
    type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findExtraMetasAccountPda } from '../pdas';
import { ABL_TOKEN_PROGRAM_ADDRESS } from '../programs';
import { getModeDecoder, getModeEncoder, type Mode, type ModeArgs } from '../types';

export const INIT_MINT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([126, 176, 233, 16, 66, 117, 209, 125]);

export function getInitMintDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_MINT_DISCRIMINATOR);
}

export type InitMintInstruction<
    TProgram extends string = typeof ABL_TOKEN_PROGRAM_ADDRESS,
    TAccountPayer extends string | AccountMeta<string> = string,
    TAccountMint extends string | AccountMeta<string> = string,
    TAccountExtraMetasAccount extends string | AccountMeta<string> = string,
    TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
    TAccountTokenProgram extends string | AccountMeta<string> = 'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb',
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountPayer extends string
                ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
                : TAccountPayer,
            TAccountMint extends string
                ? WritableSignerAccount<TAccountMint> & AccountSignerMeta<TAccountMint>
                : TAccountMint,
            TAccountExtraMetasAccount extends string
                ? WritableAccount<TAccountExtraMetasAccount>
                : TAccountExtraMetasAccount,
            TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
            TAccountTokenProgram extends string ? ReadonlyAccount<TAccountTokenProgram> : TAccountTokenProgram,
            ...TRemainingAccounts,
        ]
    >;

export type InitMintInstructionData = {
    discriminator: ReadonlyUint8Array;
    decimals: number;
    mintAuthority: Address;
    freezeAuthority: Address;
    permanentDelegate: Address;
    transferHookAuthority: Address;
    mode: Mode;
    threshold: bigint;
    name: string;
    symbol: string;
    uri: string;
};

export type InitMintInstructionDataArgs = {
    decimals: number;
    mintAuthority: Address;
    freezeAuthority: Address;
    permanentDelegate: Address;
    transferHookAuthority: Address;
    mode: ModeArgs;
    threshold: number | bigint;
    name: string;
    symbol: string;
    uri: string;
};

export function getInitMintInstructionDataEncoder(): Encoder<InitMintInstructionDataArgs> {
    return transformEncoder(
        getStructEncoder([
            ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
            ['decimals', getU8Encoder()],
            ['mintAuthority', getAddressEncoder()],
            ['freezeAuthority', getAddressEncoder()],
            ['permanentDelegate', getAddressEncoder()],
            ['transferHookAuthority', getAddressEncoder()],
            ['mode', getModeEncoder()],
            ['threshold', getU64Encoder()],
            ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
            ['symbol', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
            ['uri', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
        ]),
        value => ({ ...value, discriminator: INIT_MINT_DISCRIMINATOR }),
    );
}

export function getInitMintInstructionDataDecoder(): Decoder<InitMintInstructionData> {
    return getStructDecoder([
        ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
        ['decimals', getU8Decoder()],
        ['mintAuthority', getAddressDecoder()],
        ['freezeAuthority', getAddressDecoder()],
        ['permanentDelegate', getAddressDecoder()],
        ['transferHookAuthority', getAddressDecoder()],
        ['mode', getModeDecoder()],
        ['threshold', getU64Decoder()],
        ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
        ['symbol', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
        ['uri', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ]);
}

export function getInitMintInstructionDataCodec(): Codec<InitMintInstructionDataArgs, InitMintInstructionData> {
    return combineCodec(getInitMintInstructionDataEncoder(), getInitMintInstructionDataDecoder());
}

export type InitMintAsyncInput<
    TAccountPayer extends string = string,
    TAccountMint extends string = string,
    TAccountExtraMetasAccount extends string = string,
    TAccountSystemProgram extends string = string,
    TAccountTokenProgram extends string = string,
> = {
    payer: TransactionSigner<TAccountPayer>;
    mint: TransactionSigner<TAccountMint>;
    extraMetasAccount?: Address<TAccountExtraMetasAccount>;
    systemProgram?: Address<TAccountSystemProgram>;
    tokenProgram?: Address<TAccountTokenProgram>;
    decimals: InitMintInstructionDataArgs['decimals'];
    mintAuthority: InitMintInstructionDataArgs['mintAuthority'];
    freezeAuthority: InitMintInstructionDataArgs['freezeAuthority'];
    permanentDelegate: InitMintInstructionDataArgs['permanentDelegate'];
    transferHookAuthority: InitMintInstructionDataArgs['transferHookAuthority'];
    mode: InitMintInstructionDataArgs['mode'];
    threshold: InitMintInstructionDataArgs['threshold'];
    name: InitMintInstructionDataArgs['name'];
    symbol: InitMintInstructionDataArgs['symbol'];
    uri: InitMintInstructionDataArgs['uri'];
};

export async function getInitMintInstructionAsync<
    TAccountPayer extends string,
    TAccountMint extends string,
    TAccountExtraMetasAccount extends string,
    TAccountSystemProgram extends string,
    TAccountTokenProgram extends string,
    TProgramAddress extends Address = typeof ABL_TOKEN_PROGRAM_ADDRESS,
>(
    input: InitMintAsyncInput<
        TAccountPayer,
        TAccountMint,
        TAccountExtraMetasAccount,
        TAccountSystemProgram,
        TAccountTokenProgram
    >,
    config?: { programAddress?: TProgramAddress },
): Promise<
    InitMintInstruction<
        TProgramAddress,
        TAccountPayer,
        TAccountMint,
        TAccountExtraMetasAccount,
        TAccountSystemProgram,
        TAccountTokenProgram
    >
> {
    // Program address.
    const programAddress = config?.programAddress ?? ABL_TOKEN_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        payer: { value: input.payer ?? null, isWritable: true },
        mint: { value: input.mint ?? null, isWritable: true },
        extraMetasAccount: { value: input.extraMetasAccount ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    // Resolve default values.
    if (!accounts.extraMetasAccount.value) {
        accounts.extraMetasAccount.value = await findExtraMetasAccountPda({
            mint: getAddressFromResolvedInstructionAccount('mint', accounts.mint.value),
        });
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
    }
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb' as Address<'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'>;
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('payer', accounts.payer),
            getAccountMeta('mint', accounts.mint),
            getAccountMeta('extraMetasAccount', accounts.extraMetasAccount),
            getAccountMeta('systemProgram', accounts.systemProgram),
            getAccountMeta('tokenProgram', accounts.tokenProgram),
        ],
        data: getInitMintInstructionDataEncoder().encode(args as InitMintInstructionDataArgs),
        programAddress,
    } as InitMintInstruction<
        TProgramAddress,
        TAccountPayer,
        TAccountMint,
        TAccountExtraMetasAccount,
        TAccountSystemProgram,
        TAccountTokenProgram
    >);
}

export type InitMintInput<
    TAccountPayer extends string = string,
    TAccountMint extends string = string,
    TAccountExtraMetasAccount extends string = string,
    TAccountSystemProgram extends string = string,
    TAccountTokenProgram extends string = string,
> = {
    payer: TransactionSigner<TAccountPayer>;
    mint: TransactionSigner<TAccountMint>;
    extraMetasAccount: Address<TAccountExtraMetasAccount>;
    systemProgram?: Address<TAccountSystemProgram>;
    tokenProgram?: Address<TAccountTokenProgram>;
    decimals: InitMintInstructionDataArgs['decimals'];
    mintAuthority: InitMintInstructionDataArgs['mintAuthority'];
    freezeAuthority: InitMintInstructionDataArgs['freezeAuthority'];
    permanentDelegate: InitMintInstructionDataArgs['permanentDelegate'];
    transferHookAuthority: InitMintInstructionDataArgs['transferHookAuthority'];
    mode: InitMintInstructionDataArgs['mode'];
    threshold: InitMintInstructionDataArgs['threshold'];
    name: InitMintInstructionDataArgs['name'];
    symbol: InitMintInstructionDataArgs['symbol'];
    uri: InitMintInstructionDataArgs['uri'];
};

export function getInitMintInstruction<
    TAccountPayer extends string,
    TAccountMint extends string,
    TAccountExtraMetasAccount extends string,
    TAccountSystemProgram extends string,
    TAccountTokenProgram extends string,
    TProgramAddress extends Address = typeof ABL_TOKEN_PROGRAM_ADDRESS,
>(
    input: InitMintInput<
        TAccountPayer,
        TAccountMint,
        TAccountExtraMetasAccount,
        TAccountSystemProgram,
        TAccountTokenProgram
    >,
    config?: { programAddress?: TProgramAddress },
): InitMintInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountMint,
    TAccountExtraMetasAccount,
    TAccountSystemProgram,
    TAccountTokenProgram
> {
    // Program address.
    const programAddress = config?.programAddress ?? ABL_TOKEN_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        payer: { value: input.payer ?? null, isWritable: true },
        mint: { value: input.mint ?? null, isWritable: true },
        extraMetasAccount: { value: input.extraMetasAccount ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
    }
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb' as Address<'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'>;
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('payer', accounts.payer),
            getAccountMeta('mint', accounts.mint),
            getAccountMeta('extraMetasAccount', accounts.extraMetasAccount),
            getAccountMeta('systemProgram', accounts.systemProgram),
            getAccountMeta('tokenProgram', accounts.tokenProgram),
        ],
        data: getInitMintInstructionDataEncoder().encode(args as InitMintInstructionDataArgs),
        programAddress,
    } as InitMintInstruction<
        TProgramAddress,
        TAccountPayer,
        TAccountMint,
        TAccountExtraMetasAccount,
        TAccountSystemProgram,
        TAccountTokenProgram
    >);
}

export type ParsedInitMintInstruction<
    TProgram extends string = typeof ABL_TOKEN_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        payer: TAccountMetas[0];
        mint: TAccountMetas[1];
        extraMetasAccount: TAccountMetas[2];
        systemProgram: TAccountMetas[3];
        tokenProgram: TAccountMetas[4];
    };
    data: InitMintInstructionData;
};

export function parseInitMintInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedInitMintInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 5) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 5,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            payer: getNextAccount(),
            mint: getNextAccount(),
            extraMetasAccount: getNextAccount(),
            systemProgram: getNextAccount(),
            tokenProgram: getNextAccount(),
        },
        data: getInitMintInstructionDataDecoder().decode(instruction.data),
    };
}
