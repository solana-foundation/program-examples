/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    fixDecoderSize,
    fixEncoderSize,
    getBytesDecoder,
    getBytesEncoder,
    getStructDecoder,
    getStructEncoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
    type WritableSignerAccount,
} from '@solana/kit';
import {
    getAccountMetaFactory,
    getAddressFromResolvedInstructionAccount,
    type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findAbWalletPda, findConfigPda } from '../pdas';
import { ABL_TOKEN_PROGRAM_ADDRESS } from '../programs';

export const REMOVE_WALLET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([26, 151, 38, 109, 151, 162, 104, 28]);

export function getRemoveWalletDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(REMOVE_WALLET_DISCRIMINATOR);
}

export type RemoveWalletInstruction<
    TProgram extends string = typeof ABL_TOKEN_PROGRAM_ADDRESS,
    TAccountAuthority extends string | AccountMeta<string> = string,
    TAccountConfig extends string | AccountMeta<string> = string,
    TAccountWallet extends string | AccountMeta<string> = string,
    TAccountAbWallet extends string | AccountMeta<string> = string,
    TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountAuthority extends string
                ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
                : TAccountAuthority,
            TAccountConfig extends string ? ReadonlyAccount<TAccountConfig> : TAccountConfig,
            TAccountWallet extends string ? ReadonlyAccount<TAccountWallet> : TAccountWallet,
            TAccountAbWallet extends string ? WritableAccount<TAccountAbWallet> : TAccountAbWallet,
            TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
            ...TRemainingAccounts,
        ]
    >;

export type RemoveWalletInstructionData = { discriminator: ReadonlyUint8Array };

export type RemoveWalletInstructionDataArgs = {};

export function getRemoveWalletInstructionDataEncoder(): FixedSizeEncoder<RemoveWalletInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), value => ({
        ...value,
        discriminator: REMOVE_WALLET_DISCRIMINATOR,
    }));
}

export function getRemoveWalletInstructionDataDecoder(): FixedSizeDecoder<RemoveWalletInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getRemoveWalletInstructionDataCodec(): FixedSizeCodec<
    RemoveWalletInstructionDataArgs,
    RemoveWalletInstructionData
> {
    return combineCodec(getRemoveWalletInstructionDataEncoder(), getRemoveWalletInstructionDataDecoder());
}

export type RemoveWalletAsyncInput<
    TAccountAuthority extends string = string,
    TAccountConfig extends string = string,
    TAccountWallet extends string = string,
    TAccountAbWallet extends string = string,
    TAccountSystemProgram extends string = string,
> = {
    authority: TransactionSigner<TAccountAuthority>;
    config?: Address<TAccountConfig>;
    wallet: Address<TAccountWallet>;
    abWallet?: Address<TAccountAbWallet>;
    systemProgram?: Address<TAccountSystemProgram>;
};

export async function getRemoveWalletInstructionAsync<
    TAccountAuthority extends string,
    TAccountConfig extends string,
    TAccountWallet extends string,
    TAccountAbWallet extends string,
    TAccountSystemProgram extends string,
    TProgramAddress extends Address = typeof ABL_TOKEN_PROGRAM_ADDRESS,
>(
    input: RemoveWalletAsyncInput<
        TAccountAuthority,
        TAccountConfig,
        TAccountWallet,
        TAccountAbWallet,
        TAccountSystemProgram
    >,
    config?: { programAddress?: TProgramAddress },
): Promise<
    RemoveWalletInstruction<
        TProgramAddress,
        TAccountAuthority,
        TAccountConfig,
        TAccountWallet,
        TAccountAbWallet,
        TAccountSystemProgram
    >
> {
    // Program address.
    const programAddress = config?.programAddress ?? ABL_TOKEN_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        authority: { value: input.authority ?? null, isWritable: true },
        config: { value: input.config ?? null, isWritable: false },
        wallet: { value: input.wallet ?? null, isWritable: false },
        abWallet: { value: input.abWallet ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Resolve default values.
    if (!accounts.config.value) {
        accounts.config.value = await findConfigPda();
    }
    if (!accounts.abWallet.value) {
        accounts.abWallet.value = await findAbWalletPda({
            wallet: getAddressFromResolvedInstructionAccount('wallet', accounts.wallet.value),
        });
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('authority', accounts.authority),
            getAccountMeta('config', accounts.config),
            getAccountMeta('wallet', accounts.wallet),
            getAccountMeta('abWallet', accounts.abWallet),
            getAccountMeta('systemProgram', accounts.systemProgram),
        ],
        data: getRemoveWalletInstructionDataEncoder().encode({}),
        programAddress,
    } as RemoveWalletInstruction<
        TProgramAddress,
        TAccountAuthority,
        TAccountConfig,
        TAccountWallet,
        TAccountAbWallet,
        TAccountSystemProgram
    >);
}

export type RemoveWalletInput<
    TAccountAuthority extends string = string,
    TAccountConfig extends string = string,
    TAccountWallet extends string = string,
    TAccountAbWallet extends string = string,
    TAccountSystemProgram extends string = string,
> = {
    authority: TransactionSigner<TAccountAuthority>;
    config: Address<TAccountConfig>;
    wallet: Address<TAccountWallet>;
    abWallet: Address<TAccountAbWallet>;
    systemProgram?: Address<TAccountSystemProgram>;
};

export function getRemoveWalletInstruction<
    TAccountAuthority extends string,
    TAccountConfig extends string,
    TAccountWallet extends string,
    TAccountAbWallet extends string,
    TAccountSystemProgram extends string,
    TProgramAddress extends Address = typeof ABL_TOKEN_PROGRAM_ADDRESS,
>(
    input: RemoveWalletInput<
        TAccountAuthority,
        TAccountConfig,
        TAccountWallet,
        TAccountAbWallet,
        TAccountSystemProgram
    >,
    config?: { programAddress?: TProgramAddress },
): RemoveWalletInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountWallet,
    TAccountAbWallet,
    TAccountSystemProgram
> {
    // Program address.
    const programAddress = config?.programAddress ?? ABL_TOKEN_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        authority: { value: input.authority ?? null, isWritable: true },
        config: { value: input.config ?? null, isWritable: false },
        wallet: { value: input.wallet ?? null, isWritable: false },
        abWallet: { value: input.abWallet ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('authority', accounts.authority),
            getAccountMeta('config', accounts.config),
            getAccountMeta('wallet', accounts.wallet),
            getAccountMeta('abWallet', accounts.abWallet),
            getAccountMeta('systemProgram', accounts.systemProgram),
        ],
        data: getRemoveWalletInstructionDataEncoder().encode({}),
        programAddress,
    } as RemoveWalletInstruction<
        TProgramAddress,
        TAccountAuthority,
        TAccountConfig,
        TAccountWallet,
        TAccountAbWallet,
        TAccountSystemProgram
    >);
}

export type ParsedRemoveWalletInstruction<
    TProgram extends string = typeof ABL_TOKEN_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        authority: TAccountMetas[0];
        config: TAccountMetas[1];
        wallet: TAccountMetas[2];
        abWallet: TAccountMetas[3];
        systemProgram: TAccountMetas[4];
    };
    data: RemoveWalletInstructionData;
};

export function parseRemoveWalletInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveWalletInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 5) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 5,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            authority: getNextAccount(),
            config: getNextAccount(),
            wallet: getNextAccount(),
            abWallet: getNextAccount(),
            systemProgram: getNextAccount(),
        },
        data: getRemoveWalletInstructionDataDecoder().decode(instruction.data),
    };
}
